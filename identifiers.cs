//to make any variable we give it a name which make the programmer identify the use of that variable. 

using System;

namespace Identify
{
    class identifiers
    {
        static void Main(string[] args)
        {
            int age = 16;                   //the name of the variable itself tells it contains age of someone.
            string name = "Vyankatesh";        // the name of the variable tells the string contains the name of someone.
            string nickname = "Piyush";         // the name of the variable tells the string contains the nickname of someone.
       }
    }
}