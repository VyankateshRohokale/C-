using System;

namespace increment1
{
    class increment
    {
        static void Main(string[] args)
        {
            int no = 10;

            Console.WriteLine("before increment(initial value) : " +no);

            no++;

            Console.WriteLine("After increment : " +no);
            
        }
    }
}