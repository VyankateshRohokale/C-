using System;

namespace bool1
{
    class boolean1
    {
        static void Main(string[] args)
        {
            int a = 10;
            int b = 30;

            Console.WriteLine(a < b); // output will be true because a is smaller than b.

            Console.WriteLine(a > b); // output will be false because a is not bigger than b.

        }
    }
}